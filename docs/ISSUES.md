# Issues

The queue for problems noticed outside the task at hand: flaky gates, bugs, debt. Protocol is
ADR-0016. The orchestrator drains open entries here before taking the next plan task, one
fresh subagent and one commit per entry, so nothing noticed is lost and nothing rots.

Rules:

- A task subagent that notices a problem outside its task fixes it in place only when it is
  in code the task already changes, is a few lines, and is covered by the commit's tests
  (named in the commit message). Otherwise it **records it here and does not fix it**, in the
  same commit as its task: `scripts/record-issue.sh <kind> <where> <text>`.
- Kinds: `flaky` (a gate that failed then passed; `check.sh` records these itself), `bug`
  (observed wrong behaviour against `SPEC.md`), `debt` (something that will bite: duplication,
  a missing test, a workaround).
- An entry must be one-commit sized. If the fixing subagent finds it is larger, it adds a task
  at the top of the current milestone in `PLAN.md` describing the work, marks the entry `[x]`
  with `-> M<n>.<k>`, and commits that.
- Fixing an entry means: reproduce (a test that fails), fix, mark `[x]`, commit as
  `I-<n>: <summary>`. A flaky entry is fixed by reducing variance (warm-up, iterations, isolating
  the subject), never by raising a budget (ADR-0007).
- Entries are never deleted or edited except to mark them done or redirect them to a plan task.

Format: `- [ ] I-<n> <kind> \`<where>\` (<date>): <what was seen, and how to reproduce if known>`

- [x] I-1 flaky `perf gates` (2026-09-07): the full gate failed once on the v2 spec commit and
      passed unchanged on rerun; the failing class was not captured. Add warm-up and enough
      iterations to every `*PerfTests` class that the median is stable across three consecutive
      runs on an idle machine, and make each perf test print its median so future flake entries
      carry the number.
- [x] I-2 flaky `FSEventsWatcherTests.testX1_ownerMayDropTheWatcherWhileItsHandlerRuns` (2026-09-07): crashed with signal 5 in about one of four runs of the debug suite (the unit and smoke step of scripts/check.sh) on 2026-09-07 and passed on rerun; not captured further. Reproduce by running the debug suite repeatedly.
- [x] I-3 flaky `BacklinksSmokeTests.testK6_PF6_showWithThousandsOfBacklinksBuildsOnlyTheCapAndStaysCheap` (2026-09-08): the debug suite failed once in the M6.9 pre-commit gate with 'show with 2,000 backlinks took 26.84 ms' against a 25 ms XCTAssertLessThan, on a machine busy with the human's own session, and the same tree had passed the full gate minutes earlier; a wall-clock assertion in a debug smoke test has no warm-up or median (ADR-0007 puts timing in PerfGate).
- [x] I-4 flaky `FSEventsWatcherTests.testX1_nonNotesAndSkippedPathsAreIgnored` (2026-09-08): failed once in the M6.9 pre-commit gate with XCTAssertEqual ('[sentinel.md]' is not equal to '[]') on a loaded machine; the same tree had passed the full gate minutes earlier. The sentinel's own change seems to land in the batch the test expects to be empty when FSEvents coalesces late; reproduce by running the debug suite under load.
- [x] I-5 debt `FSEventsWatcher.fold` (2026-09-08): a folder event's scan judges the disk as it is now, which can be ahead of the event stream: a note written after the event but before the callback runs is reported added by the scan and then modified by its own event in the next callback (seen in I-4 via the replayed root creation). Benign for the index, which treats the two alike, but the note is read twice and an open note is reloaded once more than needed (X-2). Reproduce with the I-4 test under load and startWatching's settle removed.
- [x] I-6 debt `scripts/check.sh EditorPerfTests PF-3 gate` (2026-09-08): Full gate for M7.1 (test-only change) failed PF-3 twice in a row at 8.0 to 8.5 ms against the 8 ms budget while load average was 19 to 26 (another session building I-5 concurrently); the retry-once rule cannot tell machine load from a regression, so a serialized or load-aware perf run may be needed. A standalone run at 14:13 with WindowServer, Claude and WhatsApp each at 50 to 66% CPU measured 13 to 18 ms; a third run with load average 7 to 12 passed PF-3 and failed PF-4 twice at 2.29 and 2.37 s against 2 s
- [x] I-7 debt `Tests/MDNotesAppTests/WindowSnapshots.swift` (2026-09-08): V-1 snapshots capture the content view only, so a window whose content view has no opaque background (Settings) writes a transparent PNG; the dark one is white-on-clear and reads blank in a viewer. Fill windowBackgroundColor behind the view before caching so both appearances can be inspected as rendered.
- [x] I-8 bug `FSEventsWatcher / LibraryController image changes` (2026-09-08): M8.6 pass: the watcher reports only .md files, so an image file that arrives, changes or is removed under i/ on its own (iCloud sync order, a Finder drop) never reaches the index or the thumbnails: a row for a note whose embed now resolves keeps no square (or an empty one) until the note itself changes, and an inline thumbnail keeps showing the old image until an edit touches its paragraph, since NoteListController shows a cached image without re-requesting and EditorThumbnails reconciles only on edits (S-11, E-9, X-1). Reproduce: index a note embedding ![[late.png]], then write i/late.png without touching the note.
- [x] I-9 debt `Tests/MDNotesAppTests/WindowSnapshots.swift / TemplateModeSmokeTests.testV1_templateModeWindowSnapshot` (2026-09-09): M9.6 pass: the V-1 helper's light capture runs before the table has drawn a selection made on the same run-loop turn: testV1_templateModeWindowSnapshot selects row 4 and captures at once, and template-list-light.png shows no highlight on that row while template-list-dark.png (captured second, after the appearance change redraws) shows the inactive grey band. Let a run-loop turn pass (or displayIfNeeded the table's row views) in the helper before the first capture, or in the test before calling it, so both files show the same state.
- [x] I-10 debt `Sources/MDNotesCore/BodySnippet.swift` (2026-09-09): S-6 snippets strip only headings, wikilinks, embeds and fences through MarkdownScanner tokens and run their own emphasis-flanking pass; since M10.1 the scanner yields emphasis, standard links, images, autolinks, list markers, task boxes, blockquote prefixes, table pipes and rules with marker ranges (ADR-0019 makes it the single source of markdown structure), so the snippet should skip those markers from the tokens and drop its own emphasis pass
- [x] I-11 bug `LaunchPerfTests.testPF1 cold launch` (2026-09-15): the first (cold) launch in the process measured 444 to 600 ms against the 300 ms budget in 20+ attempts on 2026-09-15 with the unchanged M10.2 tree (the median of the seven launches stayed at 200 to 220 ms; a run at 09:10 the same day passed). The human's direction: do not retry the gate or make it load-aware; profile the cold path (Instruments or sample on the release test binary, compare against the last green run, look at what M8 to M10 added to launch: ThumbnailCache, TemplateStore listing, the wider MarkdownScanner in the styler and BodySnippet, EditorThumbnails reconciliation) and make cold launch faster until PF-1 passes with margin. See Q4. Profiled 2026-09-15 (I-11 pass): three regressions of ours fixed, the scan listing 20k notes through `FileManager` and `URL.resourceValues` (seven times the kernel listing, every object bridged under the Objective-C runtime lock the main thread holds while it loads frameworks), the body reads and download probes starting before the titles snapshot reached the main thread, and `NSWindowController.showWindow` soft-linking QuickLookUI through `NSDocumentController`; median launch 210 -> 85 ms, cold 450-600 -> 300-410 ms. What remains is not ours: the search field's first focus (`selectText:` -> `NSTextInputContext invalidateCharacterCoordinates` -> `+[NSCampoLightweightUIController isEligible]`) makes AppKit soft-link WritingToolsUI and 415 further images, 170-250 ms of objc registration on the main thread, ungated by `allowsWritingTools`/`writingToolsBehavior`, and a background pre-warm stalls the main thread on the runtime lock just the same (measured 406 ms). Our own cold main-thread work is now ~50 ms. The fixes are parked whole, with their MAP edits, as `docs/patches/I-11-cold-launch.patch` (`git apply docs/patches/I-11-cold-launch.patch` from the repository root, then delete the patch and commit as "I-11: cold launch: scanner via getattrlistbulk, bodies after the titles publish, showWindow without NSDocumentController (PF-1, PF-6, PF-7)"): the pre-commit gate refused them with PF-1 cold at 334 and 311 ms (median 88). Q5 answered (option 1, ADR-0020, SPEC PF-1a): apply the patch, add the Writing Tools warm-up to `LaunchPerfTests` before the first clock (test only, `NSWritingToolsCoordinator.isWritingToolsAvailable` on the main thread), delete the patch, commit; budgets untouched. Fixed 2026-09-15: patch applied and deleted; the test reads `NSWritingToolsCoordinator.isWritingToolsAvailable` once before the first clock (PF-1a), so the cold launch now measures the app's own path with the OS's Writing Tools load excluded and the median the same seven launches as before: cold 134 ms (was 444-600 unpatched, 300-410 patched), median 86 ms (was 200-220), against the unchanged 300 ms budget.
