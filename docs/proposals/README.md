# Proposals

Drafts from `/milestone <name>` conversations that have not concluded. A draft holds the
design tree so far: settled decisions and open questions. Run `/milestone <name>` again to
continue. Accepted proposals move to `accepted/` when their inbox item is written.
